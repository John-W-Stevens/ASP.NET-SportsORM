@{
    ViewData["Title"] = "Level 1";
}

<div class="well">
    <p>Using the SportsORM, complete all the following queries and show their results on Level1.cshtml.</p>
    <ol>
        <li>...all womens' leagues</li>
        <li>...all leagues where sport is any type of hockey</li>
        <li>...all leagues where sport is something OTHER THAN football</li>
        <li>...all leagues that call themselves "conferences"</li>
        <li>...all leagues in the Atlantic region</li>
        <li>...all teams based in Dallas</li>
        <li>...all teams named the Raptors</li>
        <li>...all teams whose location includes "City"</li>
        <li>...all teams whose names begin with "T"</li>
        <li>...all teams, ordered alphabetically by location</li>
        <li>...all teams, ordered by team name in reverse alphabetical order</li>
        <li>...every player with last name "Cooper"</li>
        <li>...every player with first name "Joshua"</li>
        <li>...every player with last name "Cooper" EXCEPT those with "Joshua" as the first name</li>
        <li>...all players with first name "Alexander" OR first name "Wyatt"</li>
    </ol>
</div>



<div class="row">
    <div class="col-6 offset-3">
        <h3>...all Women's Leagues</h3>
        <ul>
            @foreach (var league in ViewBag.WomensLeagues)
            {
                <li>@league.Name</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all leagues where sport is any type of Hockey</h3>
        <ul>
            @foreach (var league in ViewBag.HockeyLeagues)
            {
                <li>@league.Name</li>
            }
        </ul>
    </div>
</div>


<div class="row">
    <div class="col-6 offset-3">
        <h3>...all leagues where sport is something OTHER THAN football</h3>
        <ul>
            @foreach (var league in ViewBag.LeagueOtherThanFootball)
            {
                <li>@league.Name</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all leagues that call themselves "conferences"</h3>
        <ul>
            @foreach (var league in ViewBag.ConferenceLeagues)
            {
                <li>@league.Name</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all leagues in the Atlantic region</h3>
        <ul>
            @foreach (var league in ViewBag.AtlanticLeagues)
            {
                <li>@league.Name</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all teams based in Dallas</h3>
        <ul>
            @foreach (var team in ViewBag.DallasTeams)
            {
                <li>The @team.Location @team.TeamName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all teams named the Raptors</h3>
        <ul>
            @foreach (var team in ViewBag.RaptorTeams)
            {
                <li>The @team.Location @team.TeamName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all teams whose location includes City</h3>
        <ul>
            @foreach (var team in ViewBag.CityTeams)
            {
                <li>The @team.Location @team.TeamName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>....all teams whose names begin with "T"</h3>
        <ul>
            @foreach (var team in ViewBag.TeamsBeginningWithT)
            {
                <li>The @team.Location @team.TeamName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>....all teams, ordered alphabetically by location</h3>
        <ul>
            @foreach (var team in ViewBag.AllTeamsOrderedByLocation)
            {
                <li>The @team.Location @team.TeamName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all teams, ordered by team name in reverse alphabetical order</h3>
        <ul>
            @foreach (var team in ViewBag.AllTeamsOrderedByTeamNameReversed)
            {
                <li>The @team.Location @team.TeamName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...every player with last name "Cooper"</h3>
        <ul>
            @foreach (var player in ViewBag.PlayersWithLastNameOfCooper)
            {
                <li>@player.LastName, @player.FirstName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...every player with first name "Joshua"</h3>
        <ul>
            @foreach (var player in ViewBag.PlayersWithFirstNameOfJoshua)
            {
                <li>@player.FirstName @player.LastName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...every player with the last name of "Cooper" EXCEPT those with "Joshua" as the first name</h3>
        <ul>
            @foreach (var player in ViewBag.PlayersWithLastNameOfCooperFirstNameNotJoshua)
            {
                <li>@player.LastName, @player.FirstName</li>
            }
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-6 offset-3">
        <h3>...all players with first name "Alexander" OR first name "Wyatt"</h3>
        <ul>
            @foreach (var player in ViewBag.PlayersWithFirstNameIsAlexanderOrWyatt)
            {
                <li>@player.FirstName @player.LastName</li>
            }
        </ul>
    </div>
</div>